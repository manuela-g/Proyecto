
#include <Arduino.h>
#ifndef AGGREGATOR_H
#define AGGREGATOR_H
int aggregator  
  counter = counter + 1;
  agregator = input + agregator;
  media = agregator / counter;
  return = media;
  }
  #endif 